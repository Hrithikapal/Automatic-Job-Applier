"""
agents/nodes/field_resolver.py — Resolve form field values via profile → custom_answers → LLM → HITL.
Full implementation in Commit 8.
"""
from __future__ import annotations

from agents.state import AgentState


async def field_resolver_node(state: AgentState) -> dict:
    """STUB — replaced in Commit 8."""
    print(f"  [field_resolver] resolving fields")
    return {}
